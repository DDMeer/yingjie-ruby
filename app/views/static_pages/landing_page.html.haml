- content_for :title do
  Flexportmini Online Articles

.jumbotron.shadow.parallax
  %h1.text-shadow
    %b Take Control of Your Supply Chain
  .p-4
  - if current_user
    .p-3
    = search_form_for @ransack_courses, as: :courses_search, url: courses_path do |f|
      .input-group
        = f.search_field :title_cont, class: 'form-control-lg', style: 'float: left; width: 200px; ', placeholder: 'Find a course', :autocomplete => :off
        %span.input-group-append
          %button.btn.btn-primary.btn-lg{:type => "submit"}
            %span.fa.fa-search{"aria-hidden" => "true"}
    .p-2
  - else
    %h3.text-shadow Ship,Track,and Scale In Just a Few Clicks
    .p-2
    = link_to 'Register', new_user_registration_path, class: 'btn btn-custom-purple btn-lg'
    = link_to 'Login', new_user_session_path, class: 'btn btn-custom-purple btn-lg'
    .row.mt-1
    = link_to user_google_oauth2_omniauth_authorize_path, method: :post, class: 'btn btn-danger btn-lg mr-1' do
      Continue with
      .fab.fa-google
    .row.mt-1
    = link_to user_github_omniauth_authorize_path, method: :post, class: 'btn btn-dark btn-lg mr-1' do
      Continue with
      .fab.fa-github
    .row.mt-1
    = link_to user_facebook_omniauth_authorize_path, method: :post, class: 'btn btn-primary btn-lg mr-1' do
      Continue with
      .fab.fa-facebook

.card-group.shadow
  .card
    .card-body
      %h1
        .fa.fa-infinity
      = pluralize(Course.approved.published.count, 'online Articles')
      .row
      %small Find Out What Your Supply Chain Can Really Do
  .card
    .card-body
      %h1
        .fa.fa-infinity
      = pluralize(Course.distinct.count(:user_id), 'expert instructors')
      .row
      %small Raise the Bar on Your Supply Chain
  .card
    .card-body
      %h1
        .fa.fa-infinity
      = pluralize(User.count, 'active learners')
      .row
      %small 10,000+ Companies Choose Flexportmini
  .card
    .card-body
      %h1
        .fa.fa-infinity
      Lifetime access
      .row
      %small Lead Positive Change for the World

%p

%h2 
  .fa.fa-tags
  Top Categories
- @popular_tags.each do |tag|
  = link_to courses_path(courses_search: {course_tags_tag_id_eq: tag.id}) do
    .mt-1.btn.btn-info
      = tag.name
      %span.badge.badge-light
        = tag.course_tags_count
%hr

%h2
  .fa.fa-chart-line
  = link_to 'Popular Articles', courses_path(courses_search: {s: 'enrollments_count desc'})
.row
  - @courses.each do |course|
    = render 'courses/course_landscape', course: course
    %p

.card-group.shadow
  .card
    .card-body
      %b
        .fa.fa-clock
        Classify Products Accurately
      %br
      %small
        Enjoy lifetime access to Articles whenever you want
  .card
    .card-body
      %b
        .far.fa-check-circle
        Clear Goods Faster with Flexportmini
      %br
      %small
        Select from top instructors around the world
  .card
    .card-body
      %b
        .far.fa-play-circle
        A Fresh Approach to Clear High Value Goods
      %br
      %small
        Build your library for your career and personal growth